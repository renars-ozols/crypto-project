<nav class="navbar navbar-expand-lg navbar-light bg-light pe-5">
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ms-auto">
            <a class="nav-item nav-link active" href="/">Home</a>
            {% if auth.name %}
                <a class="nav-item nav-link" href="/dashboard">Dashboard</a>
                <a class="nav-item nav-link" href="/logout">Logout</a>
                <div class="badge text-bg-primary fs-5 my-auto ms-3">
                    <span class="me-3">{{ auth.name }}</span>
                    <i class="icon-money"></i>
                    <span class="ms-1">${{ auth.money|number_format(2) }}</span>
                </div>
            {% else %}
                <a class="nav-item nav-link" href="/register">Registration</a>
                <a class="nav-item nav-link" href="/login">Login</a>
            {% endif %}
        </div>
    </div>
</nav>