{% extends "layout.twig" %}

{% block title %}Dashboard{% endblock %}
{% block content %}
    <div class="container pt-3">
        <div class="text-center">
            <h2>Balance: ${{ auth.money|number_format(2) }}</h2>
            <div class="d-flex justify-content-center">
                <div class="me-2">
                    <form action="/dashboard/deposit" method="post">
                        <div class="text-center">
                            <label for="deposit" class="col-form-label">Amount</label>
                            <input type="number" id="deposit" name="amount" class="form-control" aria-describedby="buy">
                        </div>
                        <div class="d-grid mt-2">
                            <button class="btn btn-secondary" type="submit">Deposit</button>
                        </div>
                    </form>
                </div>
                <div class="me-2">
                    <form action="/dashboard/withdraw" method="post">
                        <div class="text-center">
                            <label for="withdraw" class="col-form-label">Amount</label>
                            <input type="number" id="withdraw" name="amount" class="form-control" aria-describedby="buy">
                        </div>
                        <div class="d-grid mt-2">
                            <button class="btn btn-outline-secondary" type="submit">Withdraw</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="mt-3">
            <h2>Portfolio:</h2>
            <table class="table mt-3">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Current value</th>
                    <th scope="col"></th>
                </tr>
                </thead>
                <tbody>
                {% for key, entry in portfolio %}
                    <tr>
                        <th scope="row">{{ key+1 }}</th>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src="{{ entry.coinLogo }}" alt="logo {{ entry.id }}" class="rounded-circle me-3" width="40px">
                                <h5 class="m-0">{{ entry.coinName }} <span class="badge bg-secondary">{{ entry.symbol }}</span></h5>
                            </div>
                        </td>
                        <td class="align-middle"><span>{{ entry.amount|number_format(2) }}</span></td>
                        <td class="align-middle"><span>need to implement</span></td>
                        <td class="align-middle"><a class="btn btn-outline-secondary" href="/coin/{{ entry.coinId }}">Trade</a></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="mt-4">
            <h2>Transactions:</h2>
            <table class="table mt-3">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Id</th>
                    <th scope="col">Type</th>
                    <th scope="col">Name</th>
                    <th scope="col">Price per coin</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Total</th>
                    <th scope="col">Date</th>
                </tr>
                </thead>
                <tbody>
                {% for key, transaction in transactions %}
                    <tr style="background-color: {{ transaction.type == 'buy' ? '#90EE90' : '#ffcccb' }}">
                        <th scope="row">{{ key+1 }}</th>
                        <td>{{ transaction.id }}</td>
                        <td>{{ transaction.type }}</td>
                        <td>{{ transaction.coinName }}</td>
                        <td>${{ transaction.coinPrice|number_format(2) }}</td>
                        <td class="align-middle"><span>{{ transaction.amount|number_format(2) }}</span></td>
                        <td class="align-middle"><span>${{ (transaction.coinPrice * transaction.amount)|number_format(2) }}</span></td>
                        <td class="align-middle"><span>{{ transaction.createdAt }}</span></td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}